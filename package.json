{
  "name": "hermesapp",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "metro": "react-native start",
    "npx-bundle": "npx react-native bundle --platform android --dev false --entry-file index.js --bundle-output android/app/src/main/assets/index.android.bundle",
    "npx-doctor": "npx react-native doctor",
    "npx-info": "npx react-native info",
    "audit": "yarn audit",
    "audit fix": "yarn audit fix",
    "audit export txt": "yarn audit --groups dependencies > audit-output.txt",
    "audit export json": "yarn audit --groups dependencies --json > audit-output.json",
    "link-assets-android": "react-native bundle --platform android --dev false --entry-file index.js --bundle-output android/app/src/main/assets/index.android.bundle --assets-dest android/app/src/main/res/",
    "regenerate-android-bundle": "react-native bundle --platform android --dev false --entry-file index.js --bundle-output android/app/src/main/assets/index.android.bundle",
    "clean-metro": "react-native start --reset-cache",
    "clean-cache": "rm -rf $TMPDIR/react-* && rm -rf $TMPDIR/metro-* && rm -rf $TMPDIR/haste-* && watchman watch-del-all && yarn cache clean",
    "clean-node-modules": "rm -rf node_modules/ && yarn install",
    "clean-fast": "yarn clean-android && yarn clean-metro",
    "clean-all": "yarn clean-android && yarn clean-node-modules && yarn clean-metro",
    "clean-android": "yarn clean-cache && rm -rf android/build && rm -rf android/app/build && rm -rf android/.gradle && cd android && ./gradlew clean && cd ..",
    "clean-gradle-caches": "yarn ./gradlew --stop && rm -rf ~/.gradle/caches/",
    "test": "jest --verbose",
    "lint": "eslint .",
    "run-android-debug": "react-native run-android",
    "run-android-release": "react-native run-android --variant=release",
    "make-apk": "yarn regenerate-android-bundle && yarn jetify && cd android && ./gradlew assemblerelease && cd ..",
    "make-aab": "yarn regenerate-android-bundle && yarn jetify && cd android && ./gradlew bundlerelease && cd ..",
    "update-react-native": "npx react-native upgrade",
    
    "env-es-dev": "ENVFILE=src/whiteLabel/es/.env",
    "run-android-es-dev-debug": "yarn env-es-dev react-native run-android --variant esDevDebug --appIdSuffix es.dev",
    "run-android-es-dev-release": "yarn regenerate-android-bundle && yarn env-es-dev react-native run-android --variant esDevRelease --appIdSuffix es.dev"
  },
  "dependencies": {
    "react": "17.0.1",
    "react-native": "0.64.2",
    "react-native-snap-carousel": "^3.9.1"
  },
  "devDependencies": {
    "@babel/core": "7.15.0",
    "@babel/runtime": "7.14.8",
    "@react-native-community/eslint-config": "2.0.0",
    "babel-jest": "26.6.3",
    "eslint": "7.14.0",
    "jest": "26.6.3",
    "metro-react-native-babel-preset": "0.64.0",
    "react-test-renderer": "17.0.1"
  },
  "jest": {
    "preset": "react-native"
  }
}
